import React from 'react'
import { useRef } from 'react'

function Canvas() {
    const divCanvas = useRef(null)
  return (
    <div id='divCanvas'>
      Canvas
    </div>
  )
}

export default Canvas
